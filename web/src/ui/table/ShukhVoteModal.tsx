// Заглушка: старый ShukhVote (голоса + исход целиком) сервер не шлёт — бюллетень
// тайный до резолва (R-8.6). Полноценная модалка на VoteView из SeatView — Task 16.
export function ShukhVoteModal() {
  return null
}
